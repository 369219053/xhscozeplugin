services:
  - type: web
    name: xhs-coze-plugin
    env: python
    region: singapore
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api_service:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health